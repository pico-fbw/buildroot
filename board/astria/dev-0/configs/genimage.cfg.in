image bootfs.vfat {
	vfat {
		files = {
@BOOT_FILES@
		}
	}

	size = 32M
}

image sdcard.img {
	hdimage {}

	partition boot {
		partition-type = 0xC
		bootable = "true"
		image = "bootfs.vfat"
	}

	partition rootfs {
		partition-type = 0x83
		image = "rootfs.ext4"
	}
}
